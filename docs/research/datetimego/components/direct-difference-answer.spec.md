# Direct difference answer

- Server-render the elapsed-day answer before the interactive calculator.
- Show the worked subtraction formula and both endpoint dates.
- Use one H2 only; the route H1 remains owned by `app/[slug]/page.tsx`.
- Reuse the WhatDateTime type, spacing, color, and card tokens.
- Do not introduce client JavaScript or new routes.

